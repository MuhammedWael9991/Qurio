<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/duration_bar"
        android:src="@drawable/duration_bar"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:scaleType="centerCrop"
        />

    <com.qurio.ui.components.DurationBar
        android:layout_width="360dp"
        android:layout_height="18dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@id/duration_bar"
        app:layout_constraintBottom_toBottomOf="@id/duration_bar"
        android:layout_marginBottom="10dp"
        android:layout_marginStart="20dp"
        />

    <TextView
        android:id="@+id/duration_text"
        style="@style/Typography.title.small"
        android:text="0 Sec"
        android:textColor="@color/On_primary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/duration_bar"
        app:layout_constraintStart_toStartOf="@id/duration_bar"
        app:layout_constraintEnd_toEndOf="@id/duration_bar"
        app:layout_constraintBottom_toBottomOf="@+id/duration_bar"
        android:layout_marginBottom="11dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>



